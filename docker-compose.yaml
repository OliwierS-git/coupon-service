services:
  postgres:

    image: postgres:16

    container_name: postgres-projekt

    environment:
      POSTGRES_DB: projektdb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123

    ports:
      - "5433:5432"

    volumes:
      - projekt-data:/var/lib/postgresql/data

volumes:
  projekt-data: